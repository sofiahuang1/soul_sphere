import 'package:flutter/material.dart';

class OneOneVoice extends StatelessWidget {
  const OneOneVoice({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('One one voice Screen'),
      ),
    );
  }
}
