import 'package:flutter/material.dart';

class InterestGroupScreen extends StatelessWidget {
  const InterestGroupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Interest group Screen'),
      ),
    );
  }
}
