import 'package:flutter/material.dart';

class ThemedRoomScreen extends StatelessWidget {
  const ThemedRoomScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Themed Screen'),
      ),
    );
  }
}
